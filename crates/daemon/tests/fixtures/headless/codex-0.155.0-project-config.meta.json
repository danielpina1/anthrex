{
  "runtime": "codex",
  "cli_version": "0.155.0",
  "captured": "2026-09-23",
  "redactions": [
    "home",
    "scratch-dirs",
    "ids"
  ],
  "note": "A scratch repository with a tracked .codex/config.toml (developer_instructions and an MCP server that writes a marker) and .codex/hooks.json (hooks that touch markers). A first turn with decision 25's flags, then exec resume on its thread. In both, the project MCP server started (its marker appeared), so Codex loads project config; the project hooks did not fire (hook trust). The first run also wrote trust_level = \"trusted\" for the repository to the user's ~/.codex/config.toml (restored afterwards). Nothing in the stream shows the project config: the evidence is the marker file. Captured with a scrubbed environment (env -i with only HOME, PATH, USER, LOGNAME, SHELL, TERM, LANG), because the capture ran inside a Claude Code session whose CLAUDE_CODE_*/CLAUDECODE variables change how a child claude behaves (transcript saving off, messaging the parent). No ANTHROPIC_API_KEY: the subscription login was used.",
  "command": "cd /tmp/fixture/s7a && codex exec --json -c 'mcp_servers.anthrex.command=\"/tmp/fixture/bin/mcp_stub.py\"' -c 'mcp_servers.anthrex.args=[]' -c mcp_servers.anthrex.tool_timeout_sec=120 -c 'mcp_servers.anthrex.default_tools_approval_mode=\"auto\"' -c 'developer_instructions=\"You are an anthrex test worker. Keep every reply to one short line.\"' -c 'model_reasoning_effort=\"low\"' -c 'approval_policy=\"never\"' -s workspace-write -c 'sandbox_workspace_write.writable_roots=[\"/tmp/fixture/s7a/.git\"]' -c sandbox_workspace_write.exclude_slash_tmp=true -c sandbox_workspace_write.exclude_tmpdir_env_var=true -- 'Run the shell command ls, then reply with the single word ok.' ; cd /tmp/fixture/s7a && codex exec resume 00000000-0000-4000-8000-000000000159 --json -c 'mcp_servers.anthrex.command=\"/tmp/fixture/bin/mcp_stub.py\"' -c 'mcp_servers.anthrex.args=[]' -c mcp_servers.anthrex.tool_timeout_sec=120 -c 'mcp_servers.anthrex.default_tools_approval_mode=\"auto\"' -c 'developer_instructions=\"You are an anthrex test worker. Keep every reply to one short line.\"' -c 'model_reasoning_effort=\"low\"' -c 'approval_policy=\"never\"' -c 'sandbox_mode=\"workspace-write\"' -c 'sandbox_workspace_write.writable_roots=[\"/tmp/fixture/s7a/.git\"]' -c sandbox_workspace_write.exclude_slash_tmp=true -c sandbox_workspace_write.exclude_tmpdir_env_var=true -- 'Run the shell command git status --short, then reply with the single word ok.'",
  "observed": true
}
